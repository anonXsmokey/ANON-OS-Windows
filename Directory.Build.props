<Project>
  <PropertyGroup>
    <!-- Keep RID-aware restore deterministic for CI and release publishing. -->
    <RuntimeIdentifiers>win-x64;win-arm64</RuntimeIdentifiers>
  </PropertyGroup>
</Project>
